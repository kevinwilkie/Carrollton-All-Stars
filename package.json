{
  "name": "carrollton-all-stars",
  "version": "1.0.0",
  "private": true,
  "description": "Carrollton All-Stars fantasy baseball league — static apps + Netlify scheduled functions",
  "scripts": {
    "test": "node --test test/unit.test.mjs",
    "test:browser": "node --test test/browser.test.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
