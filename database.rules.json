{
  "rules": {
    ".read": false,
    ".write": false,
    "draft": {
      "$channel": {
        ".read": true,
        ".write": "auth != null && auth.token.email_verified == true && auth.token.email == 'kevin.wilkie@campusoutreach.org'"
      }
    }
  }
}
